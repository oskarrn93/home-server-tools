# Break-glass internal-auth admin account (used only if Pocket ID / OAuth2 login is down).
PGADMIN_DEFAULT_EMAIL=oskar@oskarrosen.com
PGADMIN_DEFAULT_PASSWORD=changeme

# OIDC client credentials from the Pocket ID admin UI - see README.md "First-time setup".
OAUTH2_CLIENT_ID=changeme
OAUTH2_CLIENT_SECRET=changeme
