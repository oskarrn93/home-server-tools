services:
  agent:
    image: portainer/agent:2.45.0-alpine
    container_name: agent
    restart: unless-stopped
    environment:
      AGENT_CLUSTER_ADDR: agent
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /var/lib/docker/volumes:/var/lib/docker/volumes:ro
    healthcheck:
      test: ["CMD", "nc", "-z", "127.0.0.1", "9001"]
      interval: 30s
      timeout: 10s
      retries: 5

  portainer:
    image: portainer/portainer-ce:2.45.0-alpine
    container_name: portainer
    restart: unless-stopped
    command: -H tcp://agent:9001 --tlsskipverify
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:9000/"]
      interval: 30s
      timeout: 10s
      retries: 5
    volumes:
      - type: volume
        source: portainer_data
        target: /data
        volume:
          nocopy: true
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.portainer.rule=Host(`portainer.oskarrosen.io`)"
      - "traefik.http.routers.portainer.entrypoints=web,web_secure"
      - "traefik.http.routers.portainer.tls=true"
      - "traefik.http.routers.portainer.tls.certresolver=lets_encrypt"
      - "traefik.http.routers.portainer.middlewares=lan-only@file"
      - "traefik.http.services.portainer.loadbalancer.server.port=9000"
      - "traefik.docker.network=traefik_internal"
    networks:
      - default
      - traefik_internal
    depends_on:
      - agent

volumes:
  portainer_data:

networks:
  traefik_internal:
    external: true
